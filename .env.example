# ⚠️  보안 주의사항 ⚠️
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 교체하세요
# .env 파일은 절대 Git에 커밋하지 마세요!

# AWS 설정 (백엔드용)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-northeast-2
AWS_S3_BUCKET=your-s3-bucket-name

# AWS 프론트엔드 설정 (React 앱용)
REACT_APP_AWS_ACCESS_KEY_ID=your-aws-access-key-id
REACT_APP_AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
REACT_APP_AWS_REGION=ap-northeast-2
REACT_APP_S3_BUCKET=your-s3-bucket-name
REACT_APP_DYNAMODB_TABLE=DemoFactoryContents

# AWS Cognito 설정
REACT_APP_COGNITO_REGION=us-west-2
REACT_APP_COGNITO_IDENTITY_POOL_ID=your-cognito-identity-pool-id
REACT_APP_COGNITO_USER_POOL_ID=your-cognito-user-pool-id
REACT_APP_COGNITO_USER_POOL_CLIENT_ID=your-cognito-client-id

# AWS Bedrock 설정 (AI 분석용)
REACT_APP_BEDROCK_REGION=us-west-2

# AWS SES 설정
SES_FROM_EMAIL=your-email@domain.com

# 서버 포트
PORT=5000

# 개발 환경 설정
NODE_ENV=development
REACT_APP_DEBUG_MODE=false

# 보안 권장사항:
# 1. 프로덕션에서는 IAM 역할 사용 권장
# 2. 액세스 키는 최소 권한 원칙 적용
# 3. 정기적으로 액세스 키 로테이션
# 4. AWS Secrets Manager 사용 고려
